<table class="table">
  <thead>
  <th ></th>
  <th ></th>
  <th></th>
  </thead>
<% StoreDelivery.where(store_id:current_store.id).each do |d| %>
      <tr>
        <td><%= "Location : #{d.delivery_areas}" %></td><td>Price: <%= "#{d.price}" %></td> <td> <%= link_to store_delivery_path(d.id), method:'delete',remote:true do %><i style="color: red" class="fa fa-times"></i> Delete<% end %> </td>
      </tr>
<% end %>
</table>