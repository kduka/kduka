<p id="notice"><%= notice %></p>

<h1>Categories</h1>

<table class="table">
  <thead>
    <tr>
    	<th>#</th>
      <th>Name</th>
      <th>Active</th>
      <th></th>
      
    </tr>
  </thead>

  <tbody>
  	<% num = 1 %>
    <% @categories.each do |category| %>
      <tr>
      	<td><%= num %>
      	<td><%= category.name %></td>
      	<td><%= category.active %></td>
        <td><%= link_to 'Show', store_category_path(params[:store_id], category) %> | <%= link_to 'Edit', edit_store_category_path(params[:store_id],category.id) %> | <%= link_to 'Destroy', store_category_url(params[:store_id], category.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% num = num+1 %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_store_category_path %>
